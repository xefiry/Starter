[project]
name = "starter"
version = "1.3"
description = "An executable starter/stopper written in python."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psutil>=7.2.2",
    "pytest>=9.0.2",
]
